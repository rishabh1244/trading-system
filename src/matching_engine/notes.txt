order book working 

  order_list = [ (10 BTC,100 INR) , (12BTC , 120 INR), (10 BTC , 120 INR) ,(20 BTC , 140 INR)]
  order = { BUYING 40 BTC for 100 INR }



  purchase(order_list , order)
    // remove the maximum chunk possible from the order_list 
    // min(order.req_inr , order_list.INR )
    
  for i order_list.size() 
    if order_list.INR <= req_inr 
      let remaining = purchase(order_list[i], order )
      order = remaining 

