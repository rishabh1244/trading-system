TRADING ENGINE - THINGS TO FIX BEFORE PRODUCTION
====================================================
(Written in plain language so you can pick this up later)




--------------------------------------------------
NOTE ON CURRENT STATE
--------------------------------------------------
You're pausing backend fixes to build the React frontend first - that's
fine. Item #1 below needs a crash, or an insufficient-balance rejection
after a partial match, to actually bite. It won't stop you from building
or testing the frontend against the API as it is now. Just don't point
this at real users / real money until #1 and #2 are done.

--------------------------------------------------
PENDING (UNSOLVED)
--------------------------------------------------

2) ORDERS AREN'T MATCHED BY BEST PRICE FIRST
   What's wrong:
   - A real exchange matches the BEST available price first (cheapest
     ask for buyers, highest bid for sellers).
   - Your code just loops through the orders in the order they were
     added, and matches whichever one it reaches first that qualifies -
     not necessarily the best price. So a trader could get a worse deal
     than they should.

   What to do:
   - Keep your bids sorted highest-price-first, and asks sorted
     lowest-price-first (a sorted list, or a proper priority
     queue/heap).
   - Always match against the front of that sorted list first.

3) OLD FULFILLED ORDERS NEVER GET CLEANED OUT OF MEMORY
   What's wrong:
   - When an order is fully filled, you just mark it "fulfilled" and
     skip over it - but it stays in the list forever.
   - Over time this list keeps growing and the matching engine has to
     scan through more and more dead orders, getting slower and slower.

   What to do:
   - Actually remove fulfilled orders from the in-memory list once
     they're done (not just mark and skip).

4) NO TESTS
   - There isn't a single automated test for the matching engine,
     settlement logic, or auth. For something handling real money, this
     is risky - you want tests that check things like "does a partial
     fill work correctly", "does balance math stay correct after 1000
     trades", "does an insufficient-balance rejection leave the
     orderbook exactly as it was before".

5) NO RATE LIMITING
   - Nothing stops someone from spamming your login or order endpoints
     as fast as they want. Add rate limiting, especially on
     /api/login and /api/register.

6) NO LOGGING / MONITORING
   - There's basic eprintln! in a couple of places but no real
     structured logging or tracing. If something goes wrong in
     production, you'll have a hard time figuring out what happened.
     Look into the "tracing" crate.
--------------------------------------------------
COMPLETED (SOLVED)
--------------------------------------------------

9) TWO ORDERS AT THE SAME TIME COULD OVERSPEND A USER'S BALANCE
   - Fixed. Funds are checked + deducted in one atomic UPDATE
     ("... WHERE balance >= $1"), so no row is affected (and the request
     fails) if the user can't afford it. No separate check-then-deduct
     race window.

10) SEVERAL .unwrap() CALLS THAT COULD CRASH THE SERVER
    - Fixed. All .unwrap() calls have been replaced with graceful error
      handling. The one remaining .unwrap() (MDS/marketdata.rs, a
      serde_json::to_string on an internal struct) is safe - it can't
      realistically fail here.

11) SECRET KEY WAS LEAKED IN GIT HISTORY
    - Fixed. Scrubbed .env out of the entire git history with
      git-filter-repo, added .env to .gitignore, rotated JWT_SECRET, and
      added .env.example with placeholder values.

12) MONEY MATH USES FLOATING POINT NUMBERS (f64)
    - Fixed by switching all money math to rust_decimal::Decimal.
      Balances, trade qty/price, and market last_price are now Decimal.

13) A REGULAR LOCK (MUTEX) IS HELD WHILE WAITING ON THE DATABASE
    - Fixed. The orderbook mutex is only locked for the fast in-memory
      matching step and released before any database work.
    - CAUTION: don't undo this when fixing #1 above - keep the DB
      transaction and the in-memory mutex as two separate, short-lived
      locks. Don't hold the mutex open across the transaction's DB calls.

--------------------------------------------------
SUGGESTED ORDER TO TACKLE THE REMAINING PENDING ITEMS
--------------------------------------------------
1. Move fund-locking back to BEFORE matching, then wrap fund-lock +
   settle_trades in one DB transaction                            (#1)
2. Fix order matching to use best-price-first                     (#2)
3. Clean up fulfilled orders from memory                          (#3)
4. Add tests as you fix each piece above                          (#4)
5. Add rate limiting + logging                                    (#5, #6)
6. Cosmetic cleanup                                                (#7, #8)

Good luck! This is a solid learning project - the shape of the system
(auth -> orders -> matching -> settlement) is right, it just needs the
money-safety and correctness pieces tightened up before it could handle
real funds. Build the frontend now if that's more motivating - just
circle back to #1 and #2 before this ever touches real money.
